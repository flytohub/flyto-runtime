# Runtime entry points

This directory contains executable compatibility and package entry points for Flyto2 Runtime. Product behavior belongs in TypeScript source under `src/`; these files should stay thin.
