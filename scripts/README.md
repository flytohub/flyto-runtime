# Build and verification scripts

Repository build, lint, packaging, and verification helpers live here. Runtime product behavior should remain in `src/`, while scripts orchestrate repeatable repository operations.
