# Source

The Runtime server, CLI, OAuth, workspace, process, and compatibility implementation lives here. Flyto2-owned standalone capabilities are concentrated under `src/flyto2/`; upstream-compatible surfaces are preserved where migration safety requires them.
